@{
    ViewData["Title"] = "CleanApi";
}

<h1>CleanApi</h1>
<p>ASP.NET Core host for the CleanApi exercise. JSON APIs are under <code>/api</code>. For the React UI, run the Vite dev server (see README) or deploy the built SPA from IIS as documented there.</p>
<ul>
    <li><a href="/api/public/ping">GET /api/public/ping</a> (anonymous)</li>
</ul>
